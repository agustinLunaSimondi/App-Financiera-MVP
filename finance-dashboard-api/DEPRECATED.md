# DEPRECATED — Backend Node.js / Prisma

Este backend ya no está en uso activo. Toda la app actual corre contra `finance-dashboard-api-python/`.

Se mantiene en el repo solo como referencia histórica de la migración Node → Python.

**No commitear cambios acá.** Cualquier feature nueva va a `finance-dashboard-api-python/`.

Pendiente: eliminar este directorio en un commit dedicado una vez confirmado que no hay scripts externos que lo referencien.
